# 이슈 레이더 (Issue Radar) — PRD

키워드 기반 뉴스 모니터링 에이전틱 워크플로

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-09-09 |
| 과제 | Main Quest 4 — 내 도메인에서 사용할 에이전틱 워크플로 서비스 만들기 |

---

## 1. 문제 정의

### 1.1 현재 하고 있는 일

매일 도메인 관련 키워드로 뉴스를 검색해 새로운 이슈가 있는지 확인한다. 현재 절차는 이렇다.

1. 파이썬 스크립트 실행, 쉼표로 구분한 키워드 입력
2. 네이버 뉴스 검색 API 호출 (키워드별 개별 검색, OR 조건)
3. 최근 2일 이내 기사를 엑셀로 출력 (게시일시 · 제목 · 언론사 · 요약 · 링크)
4. **엑셀 100줄 이상을 눈으로 훑으며 판단**
   - 이게 어제 본 이슈인가, 새 건인가
   - 같은 이슈인데 후속보도인가
   - 보고할 가치가 있는가

현재 감시 키워드 (세트명: **사회연대경제**)

```
사회연대경제, 사회적기업, 마을기업, 소셜벤처,
임팩트얼라이언스, 한국사회적기업진흥원, 사회연대경제기본법
```

### 1.2 무엇이 문제인가

**(1) 4번이 매일 반복되고, 사람이 해야만 한다고 여겨져 왔다**

기사 100건 이상 중 실제로 서로 다른 이슈는 20건 남짓이다. 나머지는 같은 사건을 여러 매체가 받아쓴 것이거나 어제 이슈의 후속보도다. 그런데 매일 100줄을 처음부터 훑는다.

**(2) 자동화를 망설인 이유 — 누락 공포**

AI에게 맡기면 중요한 기사를 조용히 빠뜨릴까 봐 미뤄왔다. 정당한 우려다. LLM에게 "중요한 것만 골라줘"라고 시키면 실제로 누락이 발생하고, 누락됐다는 사실조차 알 수 없다.

**(3) 그런데 현재 방식에 이미 누락이 있다**

기존 수집 스크립트를 검토한 결과 두 군데에서 조용한 누락이 발생하고 있다.

| # | 위치 | 문제 | 결과 |
|---|---|---|---|
| 결함 1 | `display=100` 고정, `start` 페이징 없음 | 키워드당 최신 100건에서 잘림 | 이슈가 큰 날 후반부 기사 유실 |
| 결함 2 | `if term in item['description']` | HTML 태그 제거 **전** 원본에서 키워드 재확인 | `<b>` 태그로 분절된 복합명사 매칭 실패 |

결함 2의 원리: 네이버 뉴스 API는 검색어와 일치한 부분을 `<b>` 태그로 감싸 반환하는데, 형태소 단위로 분절해 감싸는 경우가 있다. 이때 원본 문자열에 대한 `in` 검사가 실패한다.

```
실제 응답:  <b>한국</b><b>사회적기업</b><b>진흥원</b>이 발표한...
검사:       "한국사회적기업진흥원" in "<b>한국</b><b>사회적기업</b>..."  →  False
결과:       해당 기사 폐기. 로그도 오류도 없음.
```

**(4) 실측 결과 — 결함 1이 원인이고, 결함 2는 발생하지 않았다**

2026-09-09, 실제 키워드 7개 / 최근 24시간 기준 (§8.3 가 실험).

```
합계  기존 110건 → 수정 후 187건   (77건 회복, +70%)
```

| 키워드 | 기존 코드 | 페이징만 | 페이징+태그수정 | 회복 |
|---|---|---|---|---|
| 사회적기업 | 40 | 192 | **86** | **+46** |
| 사회연대경제 | 23 | 186 | **43** | **+20** |
| 마을기업 | 15 | 218 | **26** | **+11** |
| 사회연대경제기본법 | 24 | 30 | 24 | 0 |
| 한국사회적기업진흥원 | 1 | 19 | 1 | 0 |
| 임팩트얼라이언스 | 1 | 1 | 1 | 0 |
| 소셜벤처 | 6 | 8 | 6 | 0 |

**결함 1(페이징)이 유실의 전부였다.** 회복 77건이 모두 짧고 흔한 키워드에서 나왔다. 하루치 검색만으로도 100건 천장에 걸려 70%를 못 보고 있었다.

**결함 2(태그 분절)로 인한 유실은 0건이었다.** 작성 초안에서 긴 복합명사(`한국사회적기업진흥원`, `사회연대경제기본법`, `임팩트얼라이언스`)가 형태소 분절로 폐기될 것이라 예측했으나 **실측으로 반증됐다.** 네이버는 검색어를 통째로 `<b>`로 감싸며, 이 키워드들에서는 분절이 관찰되지 않았다.

태그·엔티티 처리는 그대로 유지한다. 비용이 0이고, 발생하면 조용히 유실되는 종류의 실패를 막아주기 때문이다. 다만 **이 프로젝트가 해결한 실제 문제는 페이징이다.**

부수적으로, 재확인 로직이 제대로 작동함이 확인됐다. `마을기업`은 페이징만 하면 218건이지만 재확인 후 26건이다. 네이버 형태소 검색이 "마을"+"기업"으로 쪼개 무관한 기사까지 반환하는 것을 걸러낸 결과다.

**(5) 부수적 결함**

- 코드는 2일치(`two_days_ago`)를 받는데 의도는 12시간이었다 — 의도와 구현 불일치
- 중복 제거가 `link`만 비교해 같은 기사가 네이버뉴스 URL과 언론사 원문 URL로 중복 저장된다
- `strptime('%a, %d %b %Y')`는 로케일 의존이라 한글 Windows 환경에서 실패 가능하다 (`email.utils.parsedate_to_datetime` 권장)
- 중복 제거가 실행 내부에서만 이루어져 다음날 실행 시 어제 기사가 다시 나타난다

### 1.3 이 에이전트가 끝내주는 것

| | 내용 |
|---|---|
| **입력** | 키워드 세트, 검색 기간 |
| **처리** | 수집 → 규칙 클러스터링 → 이력 대조 → 신규/후속 판정 → 중요도 판정 |
| **결과물** | 이슈 단위로 접힌 목록 + 라벨 + 중요도 + 판단 근거 + 실행 로그 |

**기사 100건 이상이 이슈 20건 남짓으로 접히고, 각 이슈에 `신규`/`후속`/`기존` 라벨과 중요도, 그리고 그 판단의 근거가 붙는다.**

### 1.4 설계 제1원칙

> **에이전트는 기사를 지우지 않는다. 묶고, 접고, 라벨을 붙일 뿐이다.**

- 수집된 기사는 **전부 DB에 남고 전부 화면에서 접근 가능**하다
- 에이전트가 하는 일은 **필터링이 아니라 정렬과 주석**이다
- 중요도가 낮게 매겨져도 목록에서 사라지지 않는다. 순서가 뒤로 갈 뿐이다
- 라벨을 믿지 못하면 전부 펼쳐서 현재와 동일하게 훑을 수 있다

**이 원칙 덕분에 도입에 따른 손실 위험이 구조적으로 0이다.** 최악의 경우에도 현재와 같고, §1.2의 수집 결함을 고치면 재현율은 오히려 올라간다.

---

## 2. 타겟 유저

### 2.1 1차 사용자 (본인)

- 사회연대경제 영역(사회적기업, 마을기업, 소셜벤처, 관련 법·제도)의 **생태계 동향**을 매일 모니터링. 자기 조직 평판이 아니라 제도 변화와 유관 기관 움직임이 감시 대상이다
- **감시 대상이 고정되어 있지 않다.** 관심사에 따라 키워드 세트가 바뀌며, 서로 무관한 주제의 세트를 병행할 수 있다
- 기술 배경 있음 (파이썬으로 직접 수집 스크립트 작성·운영 중)
- 로컬에서 단독 사용

### 2.2 이 도구가 전제하는 사용자

- 특정 키워드군을 **반복적으로** 감시해야 하는 사람
- 놓치면 손실이 발생하는 정보를 다루는 사람 (정책 변화, 공고 마감 등)
- **자동 요약보다 누락 방지를 우선하는 사람**

### 2.3 명시적 비대상

다중 사용자, 조직 단위 배포, 실시간 알림(분 단위), 기사 원문 재배포. 배포는 과제 요건에서 제외되어 로컬 단독 실행을 전제한다.

---

## 3. 워크플로 설계

### 3.1 전체 구조 — 골격 고정 + 판단 구간 자율

```
[고정]  수집  ──▶  규칙 클러스터링  ──▶  DB 이력 대조
                                            │
                                            ▼
[자율]  ┌──────────────────────────────────────────────┐
        │  계획 → 도구 호출 → 결과 관찰 → 다음 행동      │
        │                                              │
        │  · 신규 / 후속 / 기존 판정                    │
        │  · 정보 부족 판단 → 본문 조회 또는 이력 재조회  │
        │  · 중요도 판정 + 근거 생성                     │
        │                                              │
        │  종료: 반복 3회 · 본문 3건 · 30분 · 토큰 상한   │
        └──────────────────────────────────────────────┘
                                            │
                                            ▼
[고정]  결과 표시  ──▶  사람 승인  ──▶  확정 저장 / 엑셀 출력
```

**설계 의도.** 틀릴 여지가 없는 구간(API 호출, 문자열 유사도 비교)은 코드가 결정론적으로 처리하고, 판단이 필요한 구간에서만 에이전트가 루프를 돈다. 로컬 소형 모델(2B)이 도구 선택을 자주 틀리는 문제를 구조적으로 회피하면서, 과제가 요구하는 계획→호출→관찰→결정 루프는 온전히 성립시킨다.

### 3.2 단계별 상세

| 단계 | 주체 | 하는 일 | 비고 |
|---|---|---|---|
| **1. 트리거** | 사람 / 스케줄 | 키워드 세트 선택 후 실행 | CLI로도 실행 가능 |
| **2. 수집** | 도구 | 네이버 API 호출 (키워드별 OR 검색, 페이징) | 결정론적 |
| **3. 정규화** | 코드 | **태그 제거 → 키워드 재확인** → 기간 필터 → 중복 제거 | §1.2 결함 수정 |
| **4. 규칙 클러스터링** | 코드 | 제목 유사도로 명백한 동일 사건 묶기 | LLM 비용 0, 입력 약 80% 감축 |
| **5. 이력 대조** | 도구 | 해당 세트의 최근 스레드 조회 | DB 읽기 |
| **5b. 라벨 판정** | 코드 | 제목 유사도로 신규/후속/기존 결정 | 결정론적 (§3.5) |
| **6. 중요도 판정** | **에이전트** | 중요도 등급 + 판단 근거 | 배치 3개 단위 반복 |
| **7. 심화 조사** | **에이전트 결정** | 정보 부족 시 본문 조회 | 상한 3건 |
| **8. 결과 제시** | 코드 | 이슈 단위 목록 + 라벨 + 근거 + trace | |
| **9. 승인** | **사람** | 라벨·중요도 확정, 수정 | 수정 이력 기록 |
| **10. 저장/출력** | 도구 | DB 확정 저장, 엑셀 출력 | 승인 후에만 |

### 3.3 에이전트가 판단하는 것 vs 도구가 처리하는 것

| 에이전트 (판단) | 코드·도구 (실행) |
|---|---|
| 이게 왜 중요한가, 어느 등급인가 | 네이버 API 호출 |
| 후속이면 무엇이 새로 나왔는가 | **신규/후속/기존 라벨 판정** (§3.5) |
| 제목·요약만으로 판단이 되는가 | 제목 유사도 계산 |
| **더 조사할 것인가, 여기서 끝낼 것인가** | 기사 본문 가져오기 · DB 조회/저장 · 엑셀 생성 |

### 3.4 규칙 클러스터링을 먼저 두는 이유

기사 묶기의 대부분은 LLM 없이 해결된다. 연합뉴스 기사를 여러 매체가 받아쓴 경우 제목이 거의 동일해 문자열 유사도만으로 잡힌다.

```
100건 ──[규칙: 제목 유사도]──▶ 25그룹 ──[LLM: 판단만]──▶ 결과
        0원 · 0.1초                      입력 약 1/5
```

**공짜 도구가 할 수 있는 일을 LLM에 시키지 않는다.** 하드웨어 제약(§9.1) 때문에 짜낸 우회가 아니라 비용·속도·정확도 모두에서 옳은 순서이며, 결과적으로 CPU 추론 환경에서도 실용 속도가 나온다.

### 3.5 라벨 판정을 규칙으로 두는 이유

`신규 / 후속 / 기존` 판정은 결국 **"이 묶음의 제목이 기존 스레드의 제목과 같은 사안인가"** 이며, §3.4의 유사도 함수를 *신규 묶음 × 기존 스레드*에 한 번 더 적용하면 결정론적으로 얻어진다.

§9.7의 사전 측정에서 이를 LLM에 맡겼을 때 **라벨 정확도가 5건 중 1건**이었고, 배치 뒤쪽 항목에서는 입력 문장을 그대로 복사해 근거로 반환하는 붕괴가 관찰되었다. 반면 중요도 판정은 같은 조건에서 5건 중 3건으로 상대적으로 견뎠다.

따라서 역할을 이렇게 나눈다.

| | 담당 | 근거 |
|---|---|---|
| `신규 / 후속 / 기존` | **코드 (유사도)** | 비교 연산이지 판단이 아니다. 공짜·즉시·결정론적 |
| `중요도 + 근거` | **LLM** | 도메인 맥락과 기준 해석이 필요한 실제 판단 |

라벨을 덜어내면 LLM 출력이 짧아져 배치 후반 붕괴가 완화되고 속도도 붙는다. 에이전트 루프는 그대로 유지된다 — 중요도 판정, 정보 부족 여부 판단, 본문 조회 결정, 다음 행동 결정이 모두 남는다. **판단이 필요 없는 일이 빠져 루프가 오히려 선명해진다.**

---

## 4. 도구 계획

### 4.1 도구 목록

| 도구 | 유형 | 권한 | 승인 필요 |
|---|---|---|---|
| `search_news` | 외부 API | 읽기 전용 | 아니오 |
| `lookup_history` | DB | **읽기 전용** | 아니오 |
| `fetch_article` | 웹 크롤링 | 읽기 전용 | 아니오 (상한 있음) |
| `export_excel` | 파일 생성 | 쓰기 (지정 경로만) | **예** |

**권한 최소화 원칙.** 에이전트에게 노출되는 DB 도구는 읽기 전용이다. 쓰기(라벨 확정)는 에이전트가 아니라 **사람의 승인 액션이 트리거**하며 애플리케이션 코드가 수행한다. 에이전트는 어떤 경우에도 DB를 직접 수정하거나 삭제할 수 없다.

### 4.2 입출력 스키마

#### `search_news`

```json
{
  "name": "search_news",
  "description": "네이버 뉴스 검색 API로 키워드에 해당하는 기사를 수집한다. 지정한 시간 범위 내 발행 기사만 반환한다. 키워드는 OR 조건으로 각각 개별 검색 후 합친다. 수집 단계에서 자동 호출되며, 에이전트는 판정 중 추가 확인이 필요할 때만 재호출한다.",
  "input_schema": {
    "type": "object",
    "properties": {
      "keywords":   { "type": "array", "items": { "type": "string" }, "description": "검색 키워드 목록" },
      "hours":      { "type": "integer", "default": 24, "description": "현재 시각 기준 몇 시간 이내 기사인가" },
      "max_per_kw": { "type": "integer", "default": 300, "description": "키워드당 최대 수집 건수 (API 상한 1000)" }
    },
    "required": ["keywords"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "items": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "title":            { "type": "string" },
            "description":      { "type": "string", "description": "네이버가 제공하는 요약 스니펫. LLM으로 재요약하지 않고 그대로 사용" },
            "link":             { "type": "string" },
            "origin_link":      { "type": "string" },
            "press":            { "type": "string" },
            "published_at":     { "type": "string", "format": "date-time" },
            "matched_keywords": { "type": "array", "items": { "type": "string" } }
          }
        }
      },
      "truncated_keywords": {
        "type": "array", "items": { "type": "string" },
        "description": "수집 상한에 도달해 전량을 가져오지 못한 키워드. 비어 있지 않으면 화면에 경고를 표시한다"
      },
      "failed_keywords": { "type": "array", "items": { "type": "string" } }
    }
  }
}
```

**`truncated_keywords`가 이 도구의 존재 이유 중 하나다.** §1.2 결함 1의 본질은 잘린다는 것이 아니라 **잘린 사실을 알 수 없다**는 것이었다.

#### `lookup_history`

```json
{
  "name": "lookup_history",
  "description": "현재 키워드 세트의 과거 이슈 스레드를 조회한다. 지금 판정 중인 묶음이 이미 다뤄진 이슈의 후속인지 완전히 새로운 건인지 판단할 때 사용한다. 신규/후속 판정 전에 반드시 호출한다.",
  "input_schema": {
    "type": "object",
    "properties": {
      "keyword_set_id": { "type": "integer" },
      "days":           { "type": "integer", "default": 14, "description": "며칠 전까지 조회할 것인가" },
      "query":          { "type": "string",  "description": "제목 부분 일치 검색어 (선택)" }
    },
    "required": ["keyword_set_id"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "threads": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "thread_id":  { "type": "integer" },
            "title":      { "type": "string" },
            "first_seen": { "type": "string", "format": "date" },
            "last_seen":  { "type": "string", "format": "date" },
            "importance": { "type": "string", "enum": ["최우선", "필수", "참고"] },
            "item_count": { "type": "integer" },
            "reason":     { "type": "string", "description": "확정 당시 기록된 판단 근거" }
          }
        }
      }
    }
  }
}
```

#### `fetch_article`

```json
{
  "name": "fetch_article",
  "description": "기사 본문을 가져온다. 제목과 description만으로 중요도를 판단할 수 없을 때만 사용한다. 네이버 description은 80~100자로 잘려 있어 '무엇이 어떻게 바뀌었는지'가 누락되는 경우가 있다. 특히 법·제도 변경(최우선 등급 후보)인데 개정 내용이 불명확할 때 호출한다. 느리고 실패율이 있으므로 실행당 최대 3건으로 제한된다.",
  "input_schema": {
    "type": "object",
    "properties": {
      "item_id": { "type": "integer" },
      "reason":  { "type": "string", "description": "왜 본문이 필요한지. 실행 로그에 기록되어 사람이 검토한다" }
    },
    "required": ["item_id", "reason"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "success": { "type": "boolean" },
      "content": { "type": "string", "description": "본문 텍스트 (최대 4000자로 절단)" },
      "error":   { "type": "string", "enum": ["timeout", "blocked", "parse_failed", "no_naver_link"] }
    }
  }
}
```

`reason`을 필수 항목으로 둔 것은 의도적이다. **에이전트가 왜 이 도구를 호출했는지가 실행 로그에 남아야 관찰 가능성이 성립한다.**

#### `export_excel`

```json
{
  "name": "export_excel",
  "description": "확정된 이슈 목록을 엑셀 파일로 출력한다. 사람의 승인 이후에만 호출 가능하다.",
  "input_schema": {
    "type": "object",
    "properties": {
      "run_id":      { "type": "integer" },
      "include_all": { "type": "boolean", "default": true, "description": "참고 등급 포함 여부. 기본은 전건 포함" }
    },
    "required": ["run_id"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "file_path": { "type": "string" },
      "row_count": { "type": "integer" }
    }
  }
}
```

### 4.3 실패 처리 규칙

| 도구 | 실패 유형 | 처리 |
|---|---|---|
| `search_news` | HTTP 오류 / 타임아웃 | 지수 백오프 2회 재시도 → 실패 시 해당 키워드만 건너뛰고 `failed_keywords`에 기록, **나머지 키워드는 계속 진행** |
| `search_news` | 수집 상한 도달 | 중단하지 않고 `truncated_keywords`에 기록 → **화면에 경고 표시** |
| `lookup_history` | DB 오류 | **즉시 중단.** 이력 없이 판정하면 전건이 `신규`로 잘못 라벨링되어 다음날 판단까지 오염된다 |
| `fetch_article` | 네이버 링크 없음 | 시도하지 않음 → `description`으로 판정, "본문 미확인" 표시 |
| `fetch_article` | 타임아웃 / 파싱 실패 | **재시도 없음** → `description`으로 판정, "본문 확인 실패" 표시 후 진행 |
| `export_excel` | 파일 쓰기 실패 | 오류 표시. DB 확정 상태는 유지되어 재시도 가능 |

**원칙.** 읽기 도구의 실패는 품질 저하로 흡수하고 진행한다. 단 `lookup_history` 실패는 **후속 판단 전체를 오염시키므로 중단**한다. 모든 실패는 `run_step`에 기록되어 화면에서 확인 가능하다.

---

## 5. 사람 개입 지점

### 5.1 승인 지점 — 라벨 확정

되돌리기 어려운 작업 직전, 곧 **판정 결과를 DB에 확정 저장하기 전**에 사람이 검토한다.

```
오늘의 이슈       09-09 08:00 실행 · 수집 112건 → 18묶음 · 42초 · 3,100토큰
                  ⚠ '사회적기업' 키워드가 수집 상한에 도달했습니다

     이슈                                라벨      경과       중요도     근거
 ☑ ▸ 사회연대경제기본법 국회 통과         [후속▾]  2일째·4건  [최우선▾]  B·법 제정
 ☐ ▸ 한국사회적기업진흥원 공모사업 공고    [신규▾]  2건        [최우선▾]  A·기관 직접 언급
 ☑ ▸ 사회적기업 실태조사 결과 발표        [신규▾]  3건        [필수▾]    F·통계 발표
 ☐ ▸ ○○구 마을기업 박람회 개최           [신규▾]  1건        [참고▾]    지역 행사
                                                                 [ 확정 ]
```

**라벨·중요도·근거가 이미 채워진 상태로 제시된다.** 사람은 전체를 매기는 것이 아니라 **틀린 것만 드롭다운으로 수정**한다. `▸`를 누르면 해당 묶음의 기사 전건이 펼쳐진다.

### 5.2 이 승인이 형식적이지 않은 이유

1. **확정 라벨이 내일의 판단 기준이 된다.** 오늘 확정한 스레드가 내일 `lookup_history`의 조회 대상이다. 잘못된 판정을 방치하면 다음날 판단이 연쇄적으로 틀어진다.
2. **수정 이력이 곧 실패 사례 데이터다.** "에이전트는 참고, 사람은 최우선"이 기록되어 어느 지점에서 틀리는지가 축적된다. 과제가 요구하는 실패 사례 정리가 별도 작업 없이 생성된다.

### 5.3 승인 부담의 단계적 축소

| 시기 | 검토 대상 | 근거 |
|---|---|---|
| 초기 (1~2주) | 전체 | 신뢰 형성 + 평가 데이터 수집 |
| 중기 | 최우선·필수 등급만, 나머지 자동 확정 | 수정률 안정화 이후 |
| 후기 | 에이전트가 "확신 없음"으로 표시한 것만 | 설정으로 전환 |

### 5.4 그 밖의 개입 지점

| 지점 | 내용 |
|---|---|
| 실행 전 | 키워드 세트 선택, 검색 기간 조정 |
| 실행 중 | 중단 (진행 상태는 DB에 저장되어 재개 가능) |
| 판정 후 | 특정 기사 본문 수동 조회 요청 |
| 출력 전 | 엑셀 출력 여부 및 범위 결정 |

### 5.5 재검증 시점 — 수정률 지표

키워드 세트가 바뀌면 중요도 기준도 흔들린다. 사람이 재검토 시점을 기억하는 대신 지표가 알린다.

```
[사회연대경제]  최근 30일 수정률  ████░░░░░░  24%   ⚠ 기준 점검 권장
```

| 수정률 | 해석 |
|---|---|
| 5% 내외 | 에이전트 판단이 사용자 판단과 일치. 유지 |
| 20% 이상 | 자주 어긋남. 중요도 기준 또는 프롬프트 점검 필요 |

**세트별로 따로 표시한다.** 전체 평균으로는 특정 세트의 문제가 가려진다.

---

## 6. 중요도 기준

### 6.1 초기 기준 (세트: 사회연대경제)

| 등급 | 해당 | 이 세트에서의 트리거 | 정렬 |
|---|---|---|---|
| **최우선** | **A** 우리 조직·사업이 직접 언급됨 | 이 세트에는 검색 트리거 없음 (§6.2 참조) | 최상단 |
| | **B** 법·제도 변경 (법안 통과, 시행령, 지침 개정) | **`사회연대경제기본법`** 관련 일체 | |
| **필수 확인** | **C** 예산·공모·지원사업 | 공모 공고, 예산 증감 | 그 다음 |
| | **D** 유사·경쟁 기관 동향 | **`한국사회적기업진흥원`, `임팩트얼라이언스`** | |
| | **E** 부정 이슈 (사고, 논란, 감사, 비판) | | |
| | **F** 통계·현황·실태조사 발표 | | |
| **참고** | 그 외 (지역 행사, 홍보성 기사, 단순 소개) | | 하단 |

**참고 등급도 목록에서 사라지지 않는다.** 순서만 뒤로 간다. (§1.4)

**이 세트의 성격.** `한국사회적기업진흥원`과 `임팩트얼라이언스`는 **자기 조직이 아니라 관찰 대상 기관**이다. 따라서 이 세트는 자기 조직 평판 모니터링이 아니라 **사회연대경제 생태계 동향 모니터링**이다. 이 한 줄이 `keyword_set.description`에 들어가 에이전트에게 판단 맥락으로 제공된다(§6.3).

**A등급 처리.** 이 세트에는 A등급을 발동시킬 검색 키워드가 없다. A는 검색 조건이 아니라 **판정 시 감지 조건**으로 다룬다 — 다른 키워드로 걸린 기사의 제목·요약에 지정된 조직명이 등장하면 등급을 최우선으로 승격한다. 감지 대상 조직명은 `keyword_set`에 별도 항목으로 등록하며, 등록하지 않으면 A등급은 비활성 상태로 남는다.

### 6.2 §1.2와의 연결

실측(§1.2(4)) 결과, 등급 트리거 키워드와 수집 유실의 관계는 예측과 달랐다.

| 키워드 | 등급 트리거 | 하루 건수 | 유실 |
|---|---|---|---|
| `사회연대경제기본법` | **B (최우선)** | 24 | 없음 |
| `한국사회적기업진흥원` | D (필수) | 1 | 없음 |
| `임팩트얼라이언스` | D (필수) | 1 | 없음 |
| `사회적기업` | 등급 트리거 아님 | 86 | **46건 유실** |

**등급 트리거 키워드는 건수가 적어 애초에 잘리지 않았다.** 유실은 오히려 등급과 무관한 흔한 키워드에서 발생했다.

이것이 뜻하는 바는 두 가지다. 첫째, 최우선·필수 이슈를 놓칠 위험은 예상보다 낮았다. 둘째, **그럼에도 하루 77건을 못 보고 있었으므로 그 안에 중요한 건이 섞여 있었을 가능성은 남는다** — 흔한 키워드로만 걸리는 기사가 최우선 등급일 수 없다는 보장은 없기 때문이다.

수집 로직 수정(§3.2 3단계)이 **판정 품질보다 먼저 확보해야 할 기반**이라는 결론은 유지된다. 판정이 아무리 정확해도 수집되지 않은 기사는 판정 대상에 오르지 못한다.

### 6.3 기준은 세트에 종속된다

전역 설정이 아니라 `keyword_set`의 속성이다. 세트마다 키워드·기준·이력·수정률 지표를 독립적으로 갖는다.

- **유사한 세트** — 기사가 겹칠 수 있다. `item`은 전역 1건만 저장하고 `item_thread`로 세트별 노출을 관리한다
- **전혀 다른 주제의 세트** — 이력·묶음·기준이 섞이지 않는다. 세트에 `description`(주제 설명) 칸을 두어 에이전트에게 맥락을 제공한다. 소형 모델일수록 이 한 줄이 판단 품질에 크게 작용한다

### 6.4 기준은 확정이 아니라 초안이다

```
초안 기준 → 며칠 운영 → 어긋남 발견 → 프롬프트 수정 → 재측정
                                              │
                                    이 전후 비교가 평가 산출물 (§8.3 다)
```

처음부터 완벽한 기준을 세우면 오히려 비교표를 만들 수 없다. 틀린 초안에서 시작해 고쳐나가는 것이 의도된 순서다.

---

## 7. MVP 기능과 화면 구성

### 7.1 필수 기능 (MVP)

| # | 기능 | 비고 |
|---|---|---|
| 1 | 키워드 세트 CRUD | 현재 쉼표 입력으로 하던 것 |
| 2 | 뉴스 수집 (페이징 · 태그버그 수정 · 기간 설정) | §1.2 결함 수정 |
| 3 | 규칙 클러스터링 | LLM 비용 0 |
| 4 | 에이전트 판정 루프 (신규/후속/중요도/근거) | |
| 5 | 승인 화면 + 수정 이력 기록 | |
| 6 | 실행 로그(trace) 화면 | 도구·이유·결과·소요시간 |
| 7 | 이력 조회 | |
| 8 | 엑셀 출력 | 기존 워크플로 유지 |
| 9 | CLI 실행 | 스케줄러·n8n 연결 대비 |

### 7.2 화면 4개

**① 오늘의 이슈** (메인) — §5.1 화면. 묶음 목록, 라벨·중요도 드롭다운, 펼치기, 근거, 확정 버튼, 엑셀 출력. 상단에 현재 키워드 세트를 크게 표시(다른 주제 세트 오조작 방지)하고 수집 경고(`truncated_keywords`)를 노출한다.

**② 실행 로그**

```
09-09 08:00   112건 → 18묶음   42초   3,100토큰   0원
  ├ search_news    키워드 7개 · 24시간            → 112건    8.2초
  │    ⚠ truncated: 사회적기업
  ├ lookup_history 최근 14일 스레드 조회          → 31건     0.1초
  ├ 판정 배치 1/4  묶음 5개                       → 완료    11.4초
  ├ fetch_article  "사회연대경제기본법 통과" 본문  → 성공     3.1초
  │    이유: B등급 후보이나 제정 내용이 스니펫에서 잘림
  ├ fetch_article  "진흥원 공모사업" 본문          → 실패    타임아웃
  │    대체: description으로 판정
  └ 판정 배치 4/4                                 → 완료     9.8초
```

**③ 키워드 세트** — 세트 목록/생성/전환, 키워드 편집, 주제 설명, 중요도 기준 편집, 세트별 수정률 지표.

**④ 이력** — 기간·키워드·중요도로 과거 스레드 조회. 스레드 타임라인(며칠간 어떻게 전개됐는가).

향후 게시판 감시는 `pages/` 폴더에 파일 하나를 추가하면 **⑤ 게시판 감시** 메뉴로 자동 등록된다.

---

## 8. 실험 및 평가 계획

### 8.1 평가 세트

실제 운영 데이터 **7일치**를 기준 데이터로 사용한다. 각 날짜에 대해 사용자가 직접 정답 라벨(신규/후속 · 중요도)을 부여한다.

### 8.2 측정 지표

| 지표 | 정의 | 목적 |
|---|---|---|
| **누락률** | 사용자가 최우선·필수로 본 이슈 중 에이전트가 참고 등급으로 내린 비율 | **가장 중요.** §1.2(2)의 우려를 직접 측정 |
| **수정률** | 확정 시 사람이 라벨·중요도를 고친 비율 | 일치도. 운영 중 상시 계측 |
| **묶음 정확도** | 잘못 묶인 / 잘못 나뉜 그룹 비율 | 클러스터링 품질 |
| **소요 시간** | 단계별 실행 시간 | 병목 식별 |
| **토큰·비용** | 실행당 입출력 토큰 및 환산 비용 | 로컬 0원, 비교군 API는 실비 |
| **검토 시간** | 100줄 훑기 vs 20묶음 검토 실측 | 실질 효용 |

### 8.3 비교 실험 설계

**(가) 수집 로직 — 재현율** ✅ *실행 완료 (2026-09-09, 24시간 기준)*

`python eval/run_eval.py collect`

| 세팅 | 총 수집 건수 |
|---|---|
| 기존 코드 | 110 |
| **페이징 + 태그 제거 후 검사** | **187 (+70%)** |

키워드별 분해와 해석은 §1.2(4)에 있다. 요약하면 **페이징이 유실의 전부였고 태그 분절 유실은 0건**으로, 초안의 위험도 예측은 반증됐다.

남은 확인 사항: 48시간·72시간으로 창을 넓히면 `사회적기업` 등이 1,000건 상한(`truncated_keywords`)에 닿는지. 24시간 기준으로는 닿지 않았다.

**(나) 모델** ✅ *실행 완료 (2026-09-10, 24시간 창 / 수집 77건 → 59묶음)*

`python eval/run_eval.py model --models qwen3.5:2b,qwen3:4b --seconds 3600`

두 세팅이 **같은 기사, 빈 이력**에서 출발한다. 각 세팅은 격리된 임시 DB에서 돈다 — 같은 DB를 재사용하면 앞 세팅이 만든 스레드를 뒤 세팅이 과거 이력으로 읽어 전부 `후속`으로 판정하기 때문이다.

| 세팅 | 묶음 | 판정 | 미판정 | 소요 | 토큰 | 최우선 | 필수 | 참고 | 근거 길이 |
|---|---|---|---|---|---|---|---|---|---|
| qwen3.5:2b | 59 | 57 | 2 | 943초 | 20,342 | **0** | 30 (51%) | 27 | 34자 |
| **qwen3:4b** | 59 | **59** | **0** | **861초** | 21,576 | **10** | 10 (17%) | 39 | **14자** |
| Claude Haiku 4.5 (API) | | | | | | | | | *미실행* |

**qwen3:4b가 전 항목에서 우세하다.**

1. **등급 변별력** — 2b는 최우선을 한 건도 내지 않고 절반을 `필수`로 몰았다. 등급이 정렬 기능을 하지 못한다. 4b는 최우선 10 / 필수 10 / 참고 39로 분포가 작동한다.
2. **지시 준수** — reason 25자 제한에 대해 4b는 14자, 2b는 34자.
3. **완주** — 2b는 2묶음이 미판정으로 남았고 4b는 0.
4. **속도 — 큰 모델이 더 빨랐다.** 토큰당 생성 속도는 4b가 느리다(별도 측정 3.7 vs 5.3 tok/s). 그런데 4b는 근거를 짧게 써서 생성 토큰이 적고, 그 결과 총 소요가 861초로 2b의 943초보다 짧다. **지시를 지키는 것이 속도로 돌아온 사례다.**

> **표본 크기 교훈.** 본 실험 전에 묶음 2건으로 예비 측정을 했을 때는 "4b가 2.4배 느리고 지시를 더 못 지킨다"는 정반대 결론이 나왔다. 59묶음 실험이 이를 뒤집었다. 2건 표본으로 모델을 판단하면 안 된다.

**검증되지 않은 것:** 4b의 최우선 10건이 실제로 옳은지는 확인되지 않았다. 분포가 그럴듯해진 것과 판정이 정확한 것은 다르다. 정확도는 사람이 라벨링해 산출하는 누락률(§10 수용 기준 3번)로만 확인할 수 있다.

이 결과에 따라 **기본 모델을 `qwen3:4b`로 변경**했다 (`core/llm.py`, `.env.example`).

**(다) 프롬프트**

| 세팅 | 누락률 | 수정률 |
|---|---|---|
| 중요도 기준 없음 (일반적 뉴스 중요도로 판단) | | |
| 기준 명시 (A~F 등급 + 세트 트리거) | | |
| 기준 + 사람이 고친 사례 few-shot 주입 | | |

**(라) 클러스터링 알고리즘·임계값** ✅ *실행 완료 (실데이터 109건)*

`python eval/run_eval.py cluster`

| 알고리즘 | 지표 | 임계값 | 묶음 수 | 최대 묶음 |
|---|---|---|---|---|
| 그리디(대표 1개와 비교) | Jaccard | 0.45 | **88** | 10 |
| 그리디 | 중첩계수 | 0.50 | 70 | 9 |
| **전이적 병합(union-find)** | **중첩계수** | **0.50** | **62** | **17** |

**두 가지를 다 바꿔야 했다.**

*지표* — Jaccard는 길이 차이에 과하게 벌점을 준다. 같은 사건인데도 0.12까지 떨어졌다.

```
"안양시, '2026 안양춤축제' 개막"
"춤·영화·놀이가 한자리에... '2026 안양춤축제' 18일 개막"
→ Jaccard 0.35 / 중첩계수 0.75
```

*알고리즘* — 대표 제목 하나와만 비교하는 그리디는 같은 사건을 쪼갰다. 11번가 SOVAC 기사가 `4+4+3` 세 묶음, 박람회가 `13+4` 두 묶음으로 갈렸다. 각 묶음 안은 맞는데 묶음끼리 만나지 못한 것이라 전이적 병합이 정답이었다.

수정 후 박람회 17건, 안양춤축제 14건, 11번가 13건이 각각 하나로 접혔고 체이닝 오류는 관찰되지 않았다. 남은 56개 1건짜리는 실제로 서로 다른 사건이다 — **"100건 → 20묶음"이라는 초안의 기대치가 낙관적이었다.** 실제 뉴스에는 1회성 기사의 긴 꼬리가 있다.

### 8.4 실패 사례 — 실제 관찰 기록

구현·실행 과정에서 실제로 발생한 것들이다. **다섯 건 모두 단위 테스트만으로는 드러나지 않았고, 실데이터를 넣은 뒤에야 나타났다.**

| # | 증상 | 원인 단계 | 조치 |
|---|---|---|---|
| 1 | 스레드가 이력 조회에서 통째로 사라짐 → 다음날 전건이 `신규`로 오염 | 저장 | 판정 후 갱신에 `save_thread`를 재사용해 `last_seen`을 빈 문자열로 덮어썼다. 전용 UPDATE로 분리 |
| 2 | 화면이 리런에서 죽음 | 실행 환경 | Streamlit이 리런마다 다른 스레드에서 돈다. `check_same_thread=False` |
| 3 | 109건이 88묶음 — 같은 사건이 `13+4`, `4+4+3`으로 쪼개짐 | 클러스터링 | 지표(Jaccard→중첩계수)와 알고리즘(그리디→전이적 병합)을 함께 교체 (§8.3 라) |
| 4 | 조직명 미등록인데 "현대차 직접 언급됨"으로 최우선 부여 | 프롬프트 | `(등록된 경우에만)` 단서를 2B 모델이 무시. 조건부 항목을 프롬프트에서 **아예 제거**하도록 변경 (§3.5) |
| 5 | 22배치 중 8배치(25묶음)가 조용히 미판정 | 응답 대조 | 전역 번호(40,41,42)를 준 배치에서 모델이 1,2,3으로 재부여. 배치 내 지역 번호만 노출하고 순서 폴백 추가 |

수정 후 미판정이 25건에서 1건으로 줄었다. 5건 모두 회귀 테스트를 남겼다(총 48개).

### 8.5 판정 품질 — 모델 교체로 상당 부분 해소

**문제 (qwen3.5:2b, 111건 → 64묶음)**

```
최우선 0  ·  필수 36 (56%)  ·  참고 28
```

| 문제 | 실제 사례 |
|---|---|
| **등급 인플레이션** | 절반 이상이 `필수`. 정렬해도 36줄을 봐야 하므로 등급이 기능하지 않는다 |
| **환각** | "2030 청년 제주살이하며 농촌 체험" → 근거 "B·법·제도 변경" (법 언급 없음)<br>"[기고] 집, 일자리, 공동체를 위한 지방세제" → 근거 "사회연대경제기본법 시행령 개정" (그런 사실 없음) |
| **지시 불이행** | reason 25자 제한을 반복 무시 (평균 30자, 최대 46자) |

프롬프트를 두 차례 수정했으나 오류가 A등급에서 C등급으로 자리를 옮겼을 뿐이었다. 모델 자체의 한계로 판단하고 §8.3(나) 비교 실험을 수행했다.

**결과 — `qwen3:4b`로 교체**

동일 입력(77건 → 59묶음)에서 등급 분포가 최우선 10 / 필수 10 / 참고 39로 바뀌었고, 근거 길이가 34자에서 14자로 줄었으며, 미판정이 0이 되었다. 속도도 오히려 빨라졌다. 프롬프트를 손대지 않고 모델만 바꿔 얻은 결과다.

**분포 개선은 정확도 개선이 아니었다**

4b가 최우선으로 올린 9건을 직접 열어본 결과 **거의 전부 오판이었다.**

| 기사 | 4b의 근거 | 실제 |
|---|---|---|
| 11번가, '대한민국 사회적가치 페스타' 동참 | "B·법·제도 변경" | 기업 행사 참여 |
| 충남도-한국노총, 발전공기업 통합본사 유치 맞손 | "B·법·제도 변경" | 업무협약 |
| 신안군 조직 대수술…5국 체제 전환 | "B·법 제정" | 조직 개편 |
| 전종규 동해시의원 "전담부서로 개편해야" | "B·법 제정" | 의원 발언 |
| 중장년이 오자, 마을에 새로운 일이 생겼다 | "B·법 제정" | 기획 기사 |
| 초고령사회의 '늪'…기초연금 재정부담 | "B·법 제정" | 분석 칼럼 |

**9건 전부 근거가 "B·법 제정/변경"이었고, 실제로 법이 제정된 기사는 없었다.** 2b가 전부 `필수`로 몰았다면 4b는 전부 `B`로 몰았다. 문제가 해결된 것이 아니라 자리를 옮겼다.

**지표만 보면 개선으로 보였다는 점이 이 관찰의 핵심이다.** 등급 분포(0/30/27 → 10/10/39)는 명백히 좋아졌지만 내용은 그렇지 않았다. 사람이 결과를 열어보지 않으면 알 수 없는 종류의 실패이며, §10 수용 기준 3번(누락률)이 필요한 이유가 여기에 있다.

### 8.6 대응 — 등급을 모델에게 맡기지 않는다

두 단계로 고쳤다.

**1단계. 기준 구체화** — B의 정의를 좁히고 부정 예시를 명시했다(정책 논의·조직 개편·MOU·의원 발언·기고·칼럼은 B가 아니다). 같은 9건 재판정에서 **최우선 9 → 3**.

여전히 3건이 남았고, 근거는 전부 법령명 없는 "B·법 제정"이었다. 프롬프트에 "법령 이름을 못 적으면 B가 아니다"라고 썼는데도 지켜지지 않았다.

**2단계. 구조 변경** — 모델에게 등급을 묻지 않는다.

| 이전 | 이후 |
|---|---|
| 모델이 `importance`(최우선/필수/참고)를 직접 선택 | 모델은 `code`(A~F/none)와 `law`만 응답 |
| — | 코드가 `CODE_GRADE`로 등급을 매핑 |
| — | `code=B`인데 `law`가 비면 **자동으로 참고로 강등** |

같은 9건 재판정 결과 **최우선 9 → 1**.

```
충남도-한국노총 MOU        → 참고    (code=none)
중장년 기획기사             → 참고    (code=none)
신안군 조직 개편            → 필수    (code=D)
11번가 페스타               → 필수    (code=C)
바르게살기운동              → 최우선  (code=B, law='사회연대경제기본법')
```

**지시로 안 되는 것은 구조로 막는다.** 라벨 판정을 LLM에서 규칙으로 옮긴 §3.5와 같은 처방이며, 같은 이유로 작동했다 — 작은 모델은 자유도가 줄수록 덜 흔들린다.

**남은 미검증 사항** — 마지막 1건도 옳은지는 확실하지 않다(기고문이 법을 배경으로 언급했을 뿐일 수 있다). 정확도는 여전히 사람이 라벨링해 산출하는 누락률로만 확인된다.

---

## 9. 기술 설계

### 9.1 스택

| 층 | 선택 | 이유 |
|---|---|---|
| 화면 | **Streamlit** | 별도 프론트엔드 불필요. `st.data_editor`가 승인 UI에 그대로 맞음. `pages/` 폴더로 메뉴 확장 |
| 로직 | **순수 파이썬 (`core/`)** | 화면과 분리 → CLI·스케줄러·n8n에서 동일 로직 호출 |
| 저장 | **SQLite** | 표준 라이브러리. 서버 없음. 파일 1개 |
| 판단 | **Ollama (로컬)** | OpenAI 호환 엔드포인트 사용 → 모델·제공자 교체가 주소 변경만으로 가능. structured output으로 JSON 형식 보장 |
| 루프 | **직접 구현** | 상태 관리는 SQLite가 담당. 도구 4개짜리 루프에 프레임워크는 순부담 |

**의존성 4개** — `streamlit`, `requests`, `pandas`, `xlsxwriter`. 나머지는 표준 라이브러리(`sqlite3`, `json`, `re`, `datetime`, `email.utils`, `urllib`).

**실행 환경** — AMD Ryzen 7 PRO 4750U (8C/16T) / RAM 15.3GB / **NVIDIA GPU 없음 → CPU 추론**. 이 제약이 §3.4의 규칙 우선 설계와 배치 분할 판정을 요구했다.

### 9.2 파일 구조

```
main.py              CLI 진입점
app.py               Streamlit 메인 (오늘의 이슈)
pages/
  2_실행로그.py
  3_키워드세트.py
  4_이력.py
core/
  collect.py         네이버 수집 (페이징 · 태그버그 수정)
  cluster.py         규칙 클러스터링
  agent.py           에이전트 루프
  tools.py           도구 정의 (스키마 + description)
  llm.py             모델 호출 추상화 (structured output)
  db.py              SQLite
schema.sql
.env.example         API 키 템플릿
PRD.md
README.md
```

### 9.3 DB 스키마

| 테이블 | 역할 | 주요 칸 |
|---|---|---|
| `item` | 수집 항목 원본 (전역, 중복 저장 없음) | `id`, `source`, `title`, `description`, `link`, `origin_link`, `press`, `published_at`, `collected_at`, `dedup_key`(UNIQUE), `matched_keywords` |
| `thread` | 묶음 (세트 소속) | `id`, `keyword_set_id`, `title`, `first_seen`, `last_seen`, `label`, `importance`, `reason`, `confirmed_at` |
| `item_thread` | 연결 (N:M) | `item_id`, `thread_id` |
| `keyword_set` | 세트 | `id`, `name`, `description`, `keywords`, `criteria`, `is_active` |
| `run` | 실행 1건 | `id`, `keyword_set_id`, `started_at`, `finished_at`, `item_count`, `thread_count`, `tokens_in`, `tokens_out`, `cost`, `status` |
| `run_step` | 실행 trace | `id`, `run_id`, `seq`, `tool`, `reason`, `input`, `output_summary`, `duration_ms`, `success`, `error` |

**설계 결정 3가지**

1. **`item`은 전역, `thread`는 세트 소속, `item_thread`로 연결.** 세트를 번갈아 운영할 때 전역 중복 제거를 쓰면 "세트 A가 먼저 수집한 기사가 세트 B에서는 보이지 않는" 조용한 누락이 발생한다. "본 적 있나"는 반드시 세트 단위로 판정해야 한다.
2. **`source` 컬럼.** 현재 값은 `naver_news` 하나뿐이다. 게시판 확장 시 `board_*`가 추가되며 마이그레이션이 발생하지 않는다. 컬럼 하나는 추상화가 아니므로 지금 두는 비용이 0이다.
3. **보관 기한 없음.** 항목 1건이 약 1KB, 연간 40MB 수준이다. 삭제 정책·아카이빙을 만들지 않는다. 누적 데이터는 이력 조회와 추이 분석의 자산이다.

### 9.4 종료 조건

무한 루프 및 비용 폭주 방지.

| 조건 | 값 |
|---|---|
| 판정 루프 최대 반복 | 묶음당 3회 |
| `fetch_article` 최대 호출 | 실행당 3건 |
| 실행 시간 상한 | 30분 (초과 시 미판정 묶음은 `보류` 표시 후 종료) |
| 토큰 상한 | 실행당 50,000 토큰 |

**상한 도달은 실패가 아니라 기록되는 상태다.** `run.status`에 남고 화면에 표시되며, 미판정 묶음도 목록에서 사라지지 않는다.

### 9.5 상태 관리와 재개

각 배치 판정 결과를 즉시 DB에 기록한다. 중단 시 `run.status = 'interrupted'`로 남고, 재실행하면 미판정 묶음부터 이어서 진행한다. 이미 판정된 묶음은 재계산하지 않는다.

### 9.6 보안

| 항목 | 조치 |
|---|---|
| 네이버 API 키 | `.env`로 분리, `.gitignore` 등록, `.env.example`만 커밋 |
| **기존 노출 키** | **재발급 필요.** 기존 스크립트에 하드코딩되어 있었으며 외부에 노출된 이력이 있다 |
| 에이전트 DB 권한 | 읽기 전용 도구만 노출. 쓰기는 사람의 승인이 트리거 |
| 파일 쓰기 | 지정된 출력 디렉터리로 제한 |
| 크롤링 | robots.txt 및 이용약관 준수, 요청 간 지연 삽입 |

### 9.7 사전 측정 결과 (구현 전 검증)

구현에 착수하기 전, 계획 전체가 의존하는 가정 — **"qwen3.5:2b가 JSON 스키마를 지켜 출력한다"** — 을 실측했다.

**측정 1. thinking 모드의 영향** (입력 35토큰의 최소 프롬프트)

| 설정 | 소요 | 출력 토큰 | thinking | `content` |
|---|---|---|---|---|
| 기본값 (thinking 활성) | **569.1초** | 4,061 | 15,939자 | **빈 문자열** |
| **`think: False`** | **6.9초** | 23 | 0 | 유효한 JSON |

`qwen3.5`는 thinking 모델이며 Ollama에서 기본 활성이다. 추론 토큰을 수천 개 소모한 뒤 `content`를 비워 반환하므로 **`think: False`는 선택이 아니라 필수 설정**이다. 82배 차이가 난다.

**측정 2. 실제 판정 형태** (묶음 5개 배치, `think: False`)

```
65초 · 입력 498 · 출력 371 · 스키마 위반 0건 · 요청 5건 전부 반환
```

| 항목 | 결과 |
|---|---|
| 구조화 출력 | **완전 신뢰 가능** — enum 위반 0건, 개수 정확 |
| 중요도 정확도 | 5건 중 3건 |
| **라벨 정확도** | **5건 중 1건** |
| 배치 후반 거동 | 3·4·5번의 `reason`이 입력 문장을 그대로 복사한 동일 문자열 |

**결론 3가지**

1. `think: False`를 `core/llm.py`에 고정한다 (§9.1)
2. 라벨 판정을 LLM에서 코드로 이관한다 (§3.5)
3. 배치 크기를 5에서 **3**으로 줄인다 — 후반 붕괴가 5개 시점에 이미 나타난다

이 측정 자체가 §8.3의 첫 번째 실험 데이터이며, thinking 설정과 배치 크기는 이후 비교 실험의 변수로 남는다.

**측정 3. 실제 운영 속도** (실데이터 111건 → 64묶음, `think: False`)

| 단계 | 소요 |
|---|---|
| 수집 (키워드 7개, 페이징 포함) | 8초 |
| 클러스터링 + 이력 대조 | 1초 미만 |
| 판정 (배치 3개 단위 × 22회) | **배치당 약 29초 → 합계 약 11분** |

배치당 29초는 초안 추정(13초)의 두 배였다. 생성 토큰 수가 지배적이므로 reason 길이 제한이 속도에 직결된다. 이에 따라 실행 시간 상한을 5분에서 20분으로 올렸고, 이후 `qwen3:4b`로 교체하면서(§8.3 나) 배치당 60~80초가 되어 **30분**으로 다시 조정했다 — 매일 한 번 도는 배경 작업이므로 허용 가능하며, 상한에 닿아도 미판정 묶음은 목록에 남고 `--resume`으로 이어진다.

---

## 10. 수용 기준

배포 대신 **로컬 실행 기준**으로 정의한다.

| # | 기준 | 판정 |
|---|---|---|
| 1 | README의 절차만으로 새 환경에서 실행된다 | 통과/실패 |
| 2 | 대표 시나리오 7일치 중 **5일 이상**이 사람 개입 없이 판정 완료된다 | |
| 3 | **누락률 10% 이하** — 사용자가 최우선·필수로 본 이슈 중 참고 등급으로 내려간 비율 | |
| 4 | 수집 재현율이 기존 코드 대비 **개선됨이 수치로 확인된다** (§8.3 가) | |
| 5 | 모든 도구 호출이 이유·결과·소요시간과 함께 화면에서 확인된다 | |
| 6 | 실행 중단 후 재실행 시 이어서 진행된다 | |
| 7 | 검토 시간이 기존 대비 단축됨이 실측된다 | |

**3번과 4번이 이 프로젝트의 핵심 수용 기준이다.** 도입을 망설인 이유(§1.2(2))를 직접 반증하는 지표다.

### 10.1 점검 결과 (2026-09-10)

| # | 기준 | 판정 | 근거 |
|---|---|---|---|
| 1 | README 절차만으로 새 환경 실행 | **부분** | 의존성 4개와 CLI 명령은 이 환경에서 검증. 다른 PC에서는 미검증 |
| 2 | 시나리오 중 사람 개입 없이 판정 완료 | **부분** | 1일치 실행에서 `status=done`, **63묶음 전건 판정, 미판정 0건**. 7일 표본은 미확보 |
| 3 | **누락률 10% 이하** | **미측정** | 확정 0건. 사람이 승인해야 산출된다 |
| 4 | **수집 재현율 개선 수치 확인** | **통과** | 110건 → 187건 (+70%). §1.2(4) |
| 5 | 도구 호출이 이유·결과·소요시간과 함께 화면에 | **통과** | 23단계 전부 기록. `search_news` 1 / `lookup_history` 1 / `judge` 21, 이유·소요시간 누락 0 |
| 6 | 중단 후 재실행 시 이어서 진행 | **통과** | 20분 상한 도달로 20묶음이 `held`로 남았고, `--resume 1`로 마저 판정해 65/65 완료 |
| 7 | 검토 시간 단축 실측 | **미측정** | 사람이 실제로 검토해야 잴 수 있다 |

**통과 3 / 부분 2 / 미측정 2.**

미측정 2건(3번·7번)은 모두 **사람이 며칠 운영해야 나오는 값**이다. 측정 장치는 구현되어 있고(`db.miss_rate`, ③ 화면), 승인 데이터가 쌓이는 즉시 산출된다. 코드로 채울 수 있는 항목은 아니다.

부분 2건 중 2번은 표본 문제이지 동작 문제가 아니다 — 실행한 1일치에서는 사람 개입 없이 전건 판정이 완료됐다.

---

## 11. 향후 확장

우선순위 순.

| 확장 | 내용 | 비용 |
|---|---|---|
| **게시판 감시** | 채용공고·사업공고·보도자료 스크래핑. `pages/`에 파일 추가, `source` 값 추가. 판정·승인·로그 계층 전부 재사용 | 중 (스크래퍼 + 공고용 프롬프트) |
| **자동 실행** | Windows 작업 스케줄러로 CLI 호출 | 낮음 |
| **n8n 연동** | 스케줄 트리거 + 코어 호출 + 슬랙·메일 발송. **코어 완성 후 마지막에 얹는다** | 낮음 |
| **MCP 서버화** | `core/` 기능을 MCP 도구로 노출 | 중 |
| **few-shot 자동 주입** | 사람이 고친 사례를 프롬프트에 자동 반영 | 중 |

### 확장에 대한 설계 입장

이 시스템의 이음매는 **수집 · 판단 · 발송** 세 곳뿐이다. 대부분의 확장은 그중 한 곳에 모듈 하나를 추가하는 일로 끝난다.

**따라서 확장을 위한 사전 코드를 작성하지 않는다.** 플러그인 구조나 소스 어댑터 인터페이스를 미리 만들지 않는다. 구현체가 하나뿐인 인터페이스는 두 번째가 등장했을 때 거의 항상 맞지 않아 다시 작성하게 된다. 이 설계가 확장 가능한 이유는 확장 지점을 만들어두어서가 아니라, **단순하고 층이 분리되어 있어서**다.

예외는 §9.3의 `source` 컬럼 하나다. 데이터 스키마만은 나중에 바꾸는 비용이 크기 때문이다.

### 게시판 확장 시 주의사항

1. 크롤링 전에 RSS·오픈API·공공데이터포털을 먼저 확인한다. HTML 파싱은 사이트 개편 시 조용히 중단된다
2. 수집 0건이 "새 글 없음"인지 "스크래퍼 고장"인지 구분되어야 한다
3. 공고는 마감이 있어 뉴스와 시급성이 다르다. 마감 임박 알림이 별도로 필요할 수 있다

---

## 부록 A. 과제 평가 항목 대응

| 평가 항목 | 대응 |
|---|---|
| 1. 도메인 워크플로 기획 | §1 문제 정의 · §2 타겟 · §3 워크플로 · §4 도구 계획 · §5 사람 개입 |
| 2. 도구 연결 | §4.1 도구 4개 · §4.2 입출력 스키마 · §4.3 실패 처리 · 권한 최소화 |
| 3. 에이전트 루프 | §3.1 루프 구조 · §9.4 종료 조건 · §9.5 상태 관리와 재개 |
| 4. 사람 개입 · 관찰 가능성 · 평가 | §5 승인 지점 · §7.2② 실행 로그 · §8 평가 계획 및 비교표 |
| 5. 배포 및 새로운 시도 | 배포 요건 제외. §11 게시판 확장 · n8n 자동 트리거 · MCP 서버화 |

## 부록 B. 용어

| 용어 | 뜻 |
|---|---|
| **item** | 수집된 항목 1건 (기사, 향후 공고). 현재 엑셀의 한 줄에 해당 |
| **thread** | 같은 사안으로 묶인 항목들. 시간축으로 이어짐 |
| **키워드 세트** | 함께 검색되는 키워드 묶음 + 그 세트 전용 중요도 기준 |
| **라벨** | 신규 / 후속 / 기존 |
| **trace** | 에이전트가 어떤 도구를 왜 호출했고 무엇을 받았는지의 기록 |
| **스키마** | 미리 정해둔 칸 구조. 표에 적용하면 DB 스키마, 도구에 적용하면 입출력 스키마 |
| **누락률** | 사람이 중요하다고 본 이슈 중 에이전트가 하위 등급으로 내린 비율 |
| **수정률** | 승인 시 사람이 에이전트의 라벨·중요도를 고친 비율 |
